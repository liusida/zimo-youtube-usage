# OpenWrt traffic quota (legacy)

**Deprecated:** this folder is an older implementation. The version in use on the router is **`OpenWrt-v2/`** (see the repo root `README.md`).

Kept here for reference only; behavior and file layout differ from v2 (e.g. no `quota_common.sh` carry logic, different `push_quota` payload). Do not deploy this tree for new setups.
