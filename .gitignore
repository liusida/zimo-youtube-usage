# Node (Server/)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Runtime data and logs (created by server at runtime)
Server/data/*
!Server/data/.gitkeep

# Local env / secrets (none committed; keep pattern for future use)
.env
.env.*
!.env.example

# OpenWrt: per-device baseline (written on router)
**/quota_start.txt

# OS
.DS_Store
Thumbs.db

# Editor / IDE
.idea/
.vscode/
*.swp
*~
