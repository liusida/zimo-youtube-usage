# Node (Server/)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Runtime data and logs (created by server at runtime)
Server/data/*
!Server/data/.gitkeep
# Baileys multi-file auth (also under Server/data/*)
Server/data/whatsapp-auth/

# Local env / secrets (none committed; keep pattern for future use)
.env
.env.*
!.env.example

# OpenWrt: per-device baseline (written on router)
**/quota_start.txt

# Local router config (copy from youtube_quota.conf.example)
OpenWrt/youtube_quota.conf
OpenWrt-v2/youtube_quota.conf

# Local deploy script (copy from upload.sh.example)
Server/upload.sh

# OS
.DS_Store
Thumbs.db

# Editor / IDE
.idea/
.vscode/
*.swp
*~
